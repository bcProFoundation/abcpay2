{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "ts_build/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "test": {},
    "type-check": {
      "dependsOn": ["^build"]
    }
  }
}
